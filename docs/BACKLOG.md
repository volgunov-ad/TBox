# Backlog — запросы пользователей

Сводный список задач, собранный из обращений. Статус по умолчанию: **не начато**.

Легенда сложности: **S** — точечное изменение, **M** — несколько подсистем, **L** — крупная фича / архитектурный сдвиг.

---

## Виджеты и панели

| # | Задача | Источник | Область кода | Сложн. |
|---|--------|----------|--------------|--------|
| W-01 | **Выравнивание текста в виджетах** — выбор положения символов: по центру / слева / справа (как в таблицах Word). Сейчас почти все плитки жёстко используют `TextAlign.Center`. | «…отображение символов было не по середине, а можно было выбрать с лева или с право…» | `WidgetConfigCodec`, `FloatingDashboardWidgetConfig`, `DashboardWidgetRenderer`, отдельные `Dashboard*Widget.kt` | M |
| W-02 | **Настраиваемый отступ иконок в сетке плиток** — padding иконок от краёв ячейки, чтобы при сетке 4×4 (ярлыки приложений) иконки не прижимались друг к другу. | «…настраиваемый отступ иконки в сетке плитки от краев сетки…» | `MainDashboardTab`, `DashboardAppLauncherWidget`, настройки вкладки «Плитки» | S |
| W-03 | **Виджет открытия багажника** — действие по тапу (mbCAN/VHAL: `DOOR_TRUNK`, remote trunk). | «…виджет открытия багажника» | `MbCanCatalog`, `WidgetsRepository`, новый `Dashboard*Widget.kt` | M |
| W-04 | **Виджет наклона зеркал при задней передаче** — чтение/переключение `MIRROR_REVERSE_TURN` (mbCAN/VHAL). | «…виджет наклона зеркал при включении задней передачи» | `MbCanCatalog`, `MBVehicleProperty`, виджет + политика команд | M |
| W-05 | **Кнопки регулировки зеркал и разблокировки дверей** — проверить/доделать управление (сейчас в каталоге mbCAN есть mirror/door property, отдельных виджетов нет). | «С кнопкой регулировки зеркал и разблокировки дверей не получается пока?» | `MbCanRepository`, `MbCanCatalog`, новые action-виджеты | M |
| W-06 | **Опция «получать данные через CAN» для остальных виджетов** — расширить паттерн `useMbCanVhal` (сейчас явно для громкости медиа) на другие dataKey, где данные доступны с шины. | «добавить для других виджетов функционал «получать данные через can"» | `WidgetsRepository.supportsUseMbCanVhal`, `DataProvider`, `MbCanRepository` | L |
| W-07 | **Копирование и вставка плавающих панелей / плиток** — duplicate конфигурации панели или отдельной плитки без ручной перенастройки. | «…копирование и вставку уже настроенных плавающих панелей/плиток…» | `FloatingOverlayController`, `WidgetPickerActivity`, `WidgetConfigCodec`, UI редактирования панелей | M |

---

## Отображение данных (CAN / TBox)

| # | Задача | Источник | Область кода | Сложн. |
|---|--------|----------|--------------|--------|
| D-01 | **Уличная температура при заглушенном двигателе** — как в TTG: продолжать показывать последнее значение, пока авто стоит с выключенным мотором (сейчас значение может пропадать). | «…когда авто заглушено температура за бортом продолжает отображаться, а в вашем нет…» | `CanFramesProcess`, `CanDataRepository.outsideTemperature`, `BackgroundService` | M |
| D-02 | **Задержка перед обнулением уличной температуры при null** — аналог уже реализованного для давления шин (~5 мин + запоминание при остановке двигателя). | «сделать для уличной температуры задержку на null, как это сделано для давления шин» | `BackgroundService` (wheel null-delay), `Settings`, `CanDataRepository` | S |
| D-03 | **Передачи: показывать номер передачи** — режим P/R/N/D уже отображается, но `gearBoxCurrentGear` не выводится (пусто или не декодируется с шины). | «…p, r, n, d показывало но номера передачи нет» | `CanFramesProcess` / mbCAN decode, `DashboardGearBoxWidget`, `DataProvider` (`gearBoxCurrentGear`) | M |
| D-04 | **Давление и температура шин через mbCAN/VHAL** — получать с головного устройства и **сохранять** так же, как при приёме через TBox (история, задержка null, запоминание при стоянке). | «давление шин и температуру шин получать через mbCAN/vhal и сохранять…» | `UniversalCanRepository`, `CanDataRepository` (`WheelPressureState`), `BackgroundService`, виджеты шин | L |
| D-05 | **Перевести поездки и заправки на mbCAN/VHAL** — уменьшить зависимость от TBox UDP для одометра, RPM, топлива и связанной логики учёта. | «перевести работу функций поездок и заправок на mbCAN/vhal» | `BackgroundService`, `TripRepository`, `fuel/`, `UniversalCanRepository`, `docs/Trips.md`, `docs/fuel-refuels-calibration.md` | L |

---

## Поездки

| # | Задача | Источник | Область кода | Сложн. |
|---|--------|----------|--------------|--------|
| T-01 | **Ручной сброс суточного пробега** — сброс по желанию пользователя, а не только автоматически по истечению времени простоя. | «…сброс суточного пробега, по моему желанию, а не по истечению времени простоя» | Уточнить целевой виджет/метрику (суточный пробег vs пробег поездки); `TripRepository`, UI поездок/виджет | M |

---

## Заправки

| # | Задача | Источник | Область кода | Сложн. |
|---|--------|----------|--------------|--------|
| F-01 | **Кнопка «Обновить данные по заправке»** — в меню «Заправки», когда появился интернет (повторная подтяжка цен АЗС / пересчёт оценок). | «…кнопку - обновить данные по заправке в меню Заправки, когда интернет появился» | `fuel/`, вкладка заправок UI, сетевой слой цен | S |

---

## Настройки и TBox

| # | Задача | Источник | Область кода | Сложн. |
|---|--------|----------|--------------|--------|
| S-01 | **Переключатель «Не подключаться к TBox»** — работа только через mbCAN/VHAL без UDP к tbox-proxy. | «сделать в Насти переключатель «не подключаться к tbox"» | `Settings.kt`, `TboxRepository`, `BackgroundService`, `docs/TBOX_PROXY_RU.md` | M |

---

## Автоматизации (крупная фича)

| # | Задача | Источник | Область кода | Сложн. |
|---|--------|----------|--------------|--------|
| A-01 | **Пользовательские автоматизации (триггер → действия)** — правила с условиями, задержками и действиями: всё, что уже умеет приложение (включая действия виджетов: климат, режим вождения, HTTP, ярлыки и т.д.). | «добавить функционал пользовательских автоматизаций (триггер - действия)…» | Новый подсистемный модуль, интеграция с `BackgroundService`, каталог действий из виджетов/mbCAN, UI редактора правил, персистентность | L |

---

## Рекомендуемый порядок (черновик)

1. **Быстрые улучшения UX:** F-01, D-02, W-02  
2. **Исправления отображения:** D-03, D-01  
3. **Новые action-виджеты:** W-03, W-04, W-05  
4. **Расширение панелей:** W-01, W-07  
5. **CAN-миграция данных:** D-04, D-05, S-01, W-06  
6. **Поездки:** T-01 (после уточнения метрики)  
7. **Автоматизации:** A-01 (отдельный эпик)

---

## Открытые вопросы

- **T-01:** что именно считается «суточным пробегом» в UI — отдельный счётчик, пробег с полуночи, или накопленный пробег до ручного сброса?
- **W-05:** какие именно команды не срабатывают на машине пользователя (зеркала — положение vs складывание; двери — все или только багажник)?
- **D-01 vs D-02:** достаточно ли только задержки null (D-02) или нужно отдельное удержание при `rpm = 0` как в TTG?
